<Window x:Class="Bookshelf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bookshelf"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="780" 
        Width="1300"
        WindowStyle="None"       
        Background="Transparent"
        AllowsTransparency="True"
        
        x:Name="AppMainWindow"
        WindowStartupLocation="CenterScreen"
        >

    <!--<Window.Resources>
        <Style TargetType="{x:Type local:MainWindow}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Window}">
                       
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>-->

    <WindowChrome.WindowChrome>
        <WindowChrome
              
        ResizeBorderThickness="{Binding ResizeBorderThickness}"
        CaptionHeight="{Binding TitleHeight}"
        CornerRadius="0"
        GlassFrameThickness="0"
            />

    </WindowChrome.WindowChrome>
    
    <Border Padding="{Binding Path=OuterMarginSizeThikness, Mode=OneWay, FallbackValue=6}">
        <!-- Main window outline -->
        <Grid>

            <Border CornerRadius="{Binding Path=WindowCornerRadius ,Mode=OneWay, FallbackValue=10}"
                                        Background="{Binding Path=BackgroundWhiteBrush, Mode=TwoWay, FallbackValue=White}">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" Opacity="0.4" />
                </Border.Effect>
            </Border>
            
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="{Binding Path=TitleHeightGridLength, Mode=OneWay, FallbackValue=21}"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Background="red" Grid.Column="0" Panel.ZIndex="1">
                    
                </Grid>
            </Grid>

        </Grid>
    </Border>
    
</Window>
