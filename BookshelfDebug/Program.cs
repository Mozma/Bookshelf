
//using Bookshelf.Models;
//using Bookshelf.Models.Data;
//using Bookshelf.Services;
//using Microsoft.EntityFrameworkCore;

//var context = new DataContextFactory().CreateDbContext();
//var repository = new Repository<Book>();

//var list = repository.GetAll().ToList();


//var bookBinds = context.Set<BookBind>();//.Include(o => o.Author).Include(o=>o.Shelf).ToList();


//foreach (var item in bookBinds)
//{
//    Console.WriteLine($"{item.Id} {item.Author.Id}");
//}


//foreach (var item in bookBinds)
//{
//    Console.WriteLine($"{item.Id} {item.Author.Id}");
//}

